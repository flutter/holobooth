name: gif_compositor_macos

on:
  pull_request:
    paths:
      - ".github/workflows/gif_compositor_macos.yaml"
      - "gif_compositor_macos/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/gif_compositor_macos.yaml"
      - "gif_compositor_macos/**"

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      flutter_channel: stable
      flutter_version: 3.3.2
      working_directory: gif_compositor_macos
